if you dont have webpack dev server alreaady installed

```
npm install -S webpack-dev-server
npm install -g webpack-dev-server
```

for running the dev server
```
webpack-dev-server --content-base-src
```